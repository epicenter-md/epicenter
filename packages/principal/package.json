{
	"name": "@epicenter/principal",
	"version": "0.3.0",
	"license": "AGPL-3.0-or-later",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"files": [
		"src",
		"!src/**/*.test.ts"
	],
	"dependencies": {
		"arktype": "catalog:",
		"wellcrafted": "catalog:"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"description": "The branded PrincipalId that keys every storage partition: server paths, R2 prefixes, Durable Object names, local database names, and HKDF labels. Plus the pinned instance constant."
}
