# @epicenter/agent

`@epicenter/agent` is the UI-free agent loop. It streams an injected inference engine, persists finished messages through an injected by-id store, and discovers and runs capabilities through injected tool catalogs, leaving storage, transport, and presentation ownership to its host.
