---
'@epicenter/auth': minor
---

Make the framework-agnostic auth runtime publishable by moving its API and OAuth defaults into the auth package and dropping the private `@epicenter/constants` runtime dependency.
