# Release 7.7.2

## Todo
- [x] Confirm execution details for `bun run bump-version 7.7.2`
- [x] Run the version bump script and capture generated changes
- [x] Document resulting modifications and update this plan's review section

## Notes
- Script `scripts/bump-version.ts` updates package/Cargo versions, runs `cargo update -p whispering`, commits all staged changes, tags `v7.7.2`, and pushes commit + tag (force pushes the tag).
- Execution on 2025-11-13T04:31Z produced commit `chore: bump version to 7.7.2` on branch `bump-7.7.2` and pushed tag `v7.7.2`.

## Review
- Updated version fields across JS/Tauri manifests to `7.7.2`, refreshed Cargo.lock, and published commit/tag via the automation.
- No manual follow-up required; branch `bump-7.7.2` and tag `v7.7.2` exist on origin.
