# Articles

Articles explain ideas but do not decide current architecture. Verify every
claim against `docs/adr/`, the owning package README, and code. Treat a
`Historical` banner as retrieval context only. When a Vault page owns a retired
article's lineage, move the living argument there instead of updating obsolete
guidance here.
