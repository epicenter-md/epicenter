{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "trusted-app-windows-development",
	"description": "The native surface a development trusted app window reaches: unrestricted HTTP(S) egress through the Tauri HTTP plugin (ADR-0179), and the recording and transcription operations the public @epicenter/app client exposes (ADR-0186). Not a sandbox, because an app window already runs as Epicenter on the shared origin; this file is API wiring, so it names product operations and never model administration, device enumeration, or unrelated Whispering authority. Whispering remains explicit until it joins the derived catalog.",
	"local": false,
	"windows": ["app-*", "whispering"],
	"remote": {
		"urls": ["http://127.0.0.1:*"]
	},
	"permissions": [
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://*" },
				{ "url": "https://*" },
				{ "url": "http://*:*" },
				{ "url": "https://*:*" }
			]
		},
		"allow-start-recording",
		"allow-stop-recording",
		"allow-cancel-recording",
		"allow-current-recording",
		"allow-transcribe-recording",
		"allow-prewarm-model",
		"allow-get-local-transcription-readiness",
		"core:event:allow-listen",
		"core:event:allow-unlisten"
	]
}
