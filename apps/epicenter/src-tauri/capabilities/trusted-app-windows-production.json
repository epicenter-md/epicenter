{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "trusted-app-windows-production",
	"description": "Grants every production derived-catalog app window (reserved app-* label glob) the first trusted-app authority slice: unrestricted HTTP(S) egress through the Tauri HTTP plugin (ADR-0153).",
	"local": false,
	"windows": ["app-*"],
	"remote": {
		"urls": ["http://127.0.0.1:39130"]
	},
	"permissions": [
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://*" },
				{ "url": "https://*" },
				{ "url": "http://*:*" },
				{ "url": "https://*:*" }
			]
		}
	]
}
