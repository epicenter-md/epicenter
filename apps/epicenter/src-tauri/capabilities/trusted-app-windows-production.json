{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "trusted-app-windows-production",
	"description": "Grants production trusted app windows unrestricted HTTP(S) egress through the Tauri HTTP plugin (ADR-0153). Whispering remains explicit until it joins the derived catalog.",
	"local": false,
	"windows": ["app-*", "whispering"],
	"remote": {
		"urls": ["http://127.0.0.1:39130"]
	},
	"permissions": [
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://*" },
				{ "url": "https://*" },
				{ "url": "http://*:*" },
				{ "url": "https://*:*" }
			]
		}
	]
}
