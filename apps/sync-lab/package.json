{
	"name": "@epicenter/sync-lab",
	"description": "THROWAWAY. Two devices, one Durable Object, and enough surface to see a row cross the wire. Delete once the transport is settled.",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"license": "AGPL-3.0-or-later",
	"scripts": {
		"dev": "wrangler dev",
		"build": "vite build",
		"deploy": "vite build && wrangler deploy",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@epicenter/data": "workspace:*",
		"@epicenter/workspace": "workspace:*",
		"@epicenter/sqlite": "workspace:*",
		"@sqlite.org/sqlite-wasm": "catalog:",
		"@y/y": "14.0.0-rc.24"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.20.3",
		"@cloudflare/workers-types": "^4.20260405.1",
		"@types/bun": "catalog:",
		"typescript": "catalog:",
		"vite": "catalog:",
		"vitest": "^4.1.0",
		"wrangler": "catalog:"
	}
}
