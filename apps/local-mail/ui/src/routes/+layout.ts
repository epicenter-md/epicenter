// A client-only SPA. No SSR and no prerender: the browser owns routing, and
// every byte of state comes from the scoped Epicenter handle in this page.
export const ssr = false;
export const prerender = false;
